apiVersion: v1
kind: Service
metadata:
  name: weather-dashboard
  namespace: default
spec:
  type: NodePort
  selector:
    app: weather-dashboard
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30000